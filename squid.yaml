manifestVersion: subsquid.io/v0.1
name: grab-all-nft-contracts
version: 1
description: |-
  The very first evm squid from manifest  
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]
